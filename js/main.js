$(document).ready(function() {
    var game = new Game();
    game.init();

    /*var game = new Game();
    game.init();
    
    var player1 = new Player('Test1', 'left', '45', 1);
    player1.update();
    player1.setActive(true);
    
    var player2 = new Player('Test2', 'right');
    player2.update();
    
    var act = new Act('move');
    act.setFrom(new Position(1, 1));
    act.setTo(new Position(1, 2));
    
    var log = new Log();
    log.addMessage(new Act('start'));
    log.addMessage(act, player2);
    
    var controls = new Controls();
    controls.addAct(new Act('move', 'Move', function(status) {
        console.log('click.move', status);
    }));
    controls.addAct(new Act('attack', 'Attack', function(status) {
        console.log('click.attack', status);
    }));
    controls.addAct(new Act('shield', 'Shield', function(status) {
        console.log('click.shield', status);
    }));
    controls.addAct(new Act('skip', 'Skip', function(status) {
        console.log('click.skip', status);
    }));
    
    controls.init();*/

    // 1. Сделать функцонал минйона которым мы будем управлять.
    // 2. Сделать почередно игру.
    // 3. Сделать игру через сервер.
    // 4. Сделать поиск игры.
    // 5. Переделать дизайн.
    // 6. Переделать ввертску для мобилок.

    // 0.1. Ленги.
    // 0.2. Меню игры.
    // 0.3. Разные карты для игры.
    // 0.4. Регистрация.
    // 0.5. Друзья.
    // 0.6. Чат.
    // 0.7. Плантый контент.
    // 0.8. Рейтинг.
    // 0.9. Бот.
    // 0.10. Приключения с сюжетом.
});